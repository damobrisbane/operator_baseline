{
  "kind": "ImageSetConfiguration",
  "apiVersion": "mirror.openshift.io/v1alpha2",
  "archiveSize": null,
  "mirror": {
    "operators": [
      {
        "catalog": "${CATALOG}",
        "targetCatalog": "${TARGET_NAME}",
        "packages": ${PKG_CHANNELS}
      }
    ]
  }
}
